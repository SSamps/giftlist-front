import React from 'react';
import { TListGroupAnyFields } from '../../../../types/models/listGroups';

interface Props {
    group: TListGroupAnyFields;
}

const BasicListExample: React.FC<Props> = () => {
    return <div>Basic List</div>;
};

export default BasicListExample;
