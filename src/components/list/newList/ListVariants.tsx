import { Fragment } from 'react';

interface Props {}
const ListVariants: React.FC<Props> = (): JSX.Element => {
    return (
        <Fragment>
            <div>Variants go here</div>
        </Fragment>
    );
};

export default ListVariants;
